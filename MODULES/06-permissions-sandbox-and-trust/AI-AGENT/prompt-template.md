# Prompt Template

Use `ChinaSiro/claude-code-sourcemap` as the only code source.

Task:

1. Explain the permission flow from tool call to final decision.
2. Distinguish rules, UI approval, and runtime limits.
3. Use paths under `utils/permissions/` and `components/permissions/`.

