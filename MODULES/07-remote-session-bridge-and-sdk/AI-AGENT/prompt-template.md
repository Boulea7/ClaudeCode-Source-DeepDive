# Prompt Template

Use `ChinaSiro/claude-code-sourcemap` as the only code source.

Task:

1. Explain the role of `remote/` and `bridge/`.
2. Identify session management, transport, and permission callback paths.
3. Avoid relying on private-repo speculation when public source paths exist.

