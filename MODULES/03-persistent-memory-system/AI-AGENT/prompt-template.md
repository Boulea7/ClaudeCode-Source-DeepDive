# Prompt Template

Use `ChinaSiro/claude-code-sourcemap` as the only code source.

Task:

1. Separate instruction memory, durable memory, and session memory.
2. Explain how memory files enter the runtime context.
3. Explain why session memory matters for compact continuity.
4. Avoid reducing the whole system to just `CLAUDE.md`.

