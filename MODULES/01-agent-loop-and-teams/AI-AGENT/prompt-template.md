# Prompt Template

Use `ChinaSiro/claude-code-sourcemap` as the only code source.

Task:

1. Explain how `AgentTool` creates, runs, resumes, or forks agents.
2. Map `tasks/` to runtime concepts.
3. Separate confirmed code facts from inferred runtime behavior.
4. Keep explanations short and evidence-backed.

